#include <stdio.h>

/**
 * substract - adds two integers and returns the result
 * @a: int to be added to b
 * @b: int to be added to a
 * Return: difference of a and b
 */
int sub(int a, int b)
{
int c;
c = a - b;
return (c);
}
