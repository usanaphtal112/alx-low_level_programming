#include <stdio.h>

/**
 * product - adds two integers and returns the result
 * @a: int to be added to b
 * @b: int to be added to a
 * Return: product of a and b
 */
int mul(int a, int b)
{
int c;
c = a * b;
return (c);
}
