#include <stdio.h>

/**
 * Division - adds two integers and returns the result
 * @a: int to be added to b
 * @b: int to be added to a
 * Return: quatient of a and b
 */
int div(int a, int b)
{
int c;
c = a / b;
return (c);
}
